import { Button, Flex, Stack } from '@chakra-ui/react'
import { EditorLayout } from '../components/EditorLayout'

export const Editor = () => {
    return (
        <Stack>
            <EditorLayout />
        </Stack>
    )
}
